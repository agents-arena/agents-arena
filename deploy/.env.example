# Copy to .env and adjust. docker compose reads .env automatically.

# Host port the arena web UI + API are published on.
ARENA_PORT=8080

# Image release to run. Default is the latest published build; pin a specific
# version for a reproducible deployment, e.g. v0.1.0.
ARENA_VERSION=latest
